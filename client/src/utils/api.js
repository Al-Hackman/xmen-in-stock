// import this file to use api details instead of
// hardcoding in each component

const api = {
    apiUrl: "http://localhost:8080",
    warehouseEndpoint: "/warehouse",
    inventoryEndpoint: "/inventory"
};

export default api;